[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
optimizer = true
optimizer_runs = 200
solc = "0.8.24"
evm_version = "paris"

[rpc_endpoints]
sepolia = "${RPC_SEPOLIA}"
mainnet = "${RPC_MAINNET}"

[etherscan]
# Opcional: para verificación de contratos
