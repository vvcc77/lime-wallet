[package]
name = "crypto-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
falcon = "0.3"           # placeholder lattice package
kyber = "0.5"
sphincs = "0.2"
rand = "0.8"
wasm-bindgen = "0.2"

